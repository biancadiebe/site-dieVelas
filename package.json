{
  "name": "dieVelas",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./sitedie run dev",
    "build": "npm --prefix ./sitedie run build",
    "start": "npm --prefix ./sitedie run start",
    "lint": "npm --prefix ./sitedie run lint"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
